DATABASE_PATH=/app/var/database.sqlite

APP_DEBUG=false
APP_TIMEZONE=America/New_York
APP_URL=https://schedule.blade.lan

SESSION_COOKIE_SECURE=true

MAILER_DSN=smtp://mailpit:1025
MAIL_FROM=schedule@blade.lan

# Set to point page controllers at a running `npm run dev` Vite server
# instead of the built manifest (leave unset in production).
#VITE_DEV_SERVER_URL=http://127.0.0.1:5173
